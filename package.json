{
  "name": "asteroid-rush",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "render:build": "npm install && npm run build && npm prune --omit=dev"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  },
  "dependencies": {
    "stats.js": "^0.17.0",
    "three": "^0.177.0"
  }
}
